export interface SurnameEntity {
  name: string;
  slug: string;
  rank: number;
  count: number;
  prop100k: number;
  origin: string;
  censusYear: string;
  source: string;
  sampleFirstNames: string[];
}
